# Copy this file to .env and fill in real values. Never commit the real .env file.

# From the Discord Developer Portal -> your application -> Bot -> Token
DISCORD_BOT_TOKEN=

# Right-click your server icon in Discord (Developer Mode must be on) -> Copy Server ID
DISCORD_GUILD_ID=

# From console.anthropic.com -> API Keys
# OPTIONAL - leave blank to run without AI review. Items will skip straight
# from Data Entry to Queue Review with the raw submitted note. Add a key here
# any time later and restart the bot to turn AI review on - no code changes needed.
ANTHROPIC_API_KEY=

# Optional - defaults to data/pallet_tracker.db if not set
DATABASE_PATH=data/pallet_tracker.db
